const dayOfWeeks = ["Mon", "Tue", "Wed", "Tue", "Fri"];

export function getDayWithWeekend(dayLimit) {
  let countDay = 0;
  let currentDay = new Date()
  let leastDay = dayLimit - parseInt(new Date().getDate());

  for (let i = 0; i < leastDay.length; i++) {
      
      new Date()
  }

  return -1 * leastDay;
}
